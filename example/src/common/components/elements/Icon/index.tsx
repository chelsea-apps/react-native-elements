import React from 'react';
import IconElement, {
	IconProps as IconElementProps,
} from '../../../../../lib/Icon';

const Icon = ({ ...props }: IconElementProps) => <IconElement {...props} />;

export default Icon;
